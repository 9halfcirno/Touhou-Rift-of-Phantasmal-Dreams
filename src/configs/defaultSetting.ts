import { SettingValue } from "@/core/Setting"

const defaultSetting: Record<string, SettingValue> = {
	"display.resolution": 1,

	"window.aspect": "window", //16 / 9,
	"window.full_screen": false
}

export { defaultSetting }